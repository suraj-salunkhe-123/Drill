## Managing software
#Use `apt` (Ubuntu) or `homebrew` (Mac) to:


#1. Install `htop`, `vim` and `nginx`
#-> The below mwntion commands are used to install packages
#-> apt-get is a command line tool which helps in handling packages, its task is to retrive information and packages from the source of installation. and install command is sed to install the packege.
sudo apt-get install htop
sudo apt-get install vim
sudo apt-get install nginx


#2. Uninstall `nginx`
#-> In below command remove command is responsible for removing packages from the system.
sudo apt-get remove nginx
